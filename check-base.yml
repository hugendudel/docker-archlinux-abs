---
platform: "linux"

image_resource:
  type: docker-image
  source:
    repository: stinga0815/archlinux-base
    tag: latest

inputs:
  - name: "git-archlinux-abs"

run:
  path: "git-archlinux-abs/check-base.sh"
